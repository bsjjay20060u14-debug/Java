## 7.1 컬렉션과 제네릭
### 컬렉션의 개념
컬렉션은 배열이 가진 고정 크기의 단접을 극복하기 위해 객체들을 쉽게 삽입, 삭제, 검색할 수 있는 가변 크기의 컨테이너 이다.

[ 컬렉션 특징 ]
- 제네릭이라는 기법으로 구현되어 있다.
- 컬렉션의 요소는 객체들만 가능하다.

### 제네릭의 기본 개념
제네릭은 모든 종류의 타입을 다룰 수 있도록, 클래스나 메소드를 타입 매개변수를 이용해 선언하는 기법이다.

자바의 제네릭은 C++의 텔플릿과 동일하다.

제네릭은 클래스 코드를 찍어내듯이 생산할 수 있도록 일반화시키는 도구이다.

[ 제네릭 타입 매개변수 ]
- E : Element를 의마하며 컬렉션에서 요소임을 나타냄
- T : Type을 의미
- V : Value를 의미
- K : Key를 의미

## 7.2 제네릭 컬렉션 활용
### Vector<E>
Vector<E>는 배열을 가변 크기로 다룰 수 있게 하고, 객체의 삽입, 삭제, 이동이 쉽도록 구성한 컬렉션 클래스이다.

`Vector<Integer> v = new Vector<Integer>( );` 로 생성가능하다.

int, double 등의 기본 타입은 E에 사용할 수 없다. E에 구체적인 타입을 지정하지 않고 사용하면 경고 메시지가 출력된다.

벡터에는 null도 삽입할 수 있다.

Vector의 주요 메소드 (표408p)

add( )메소드를 이요하면 벡터의 끄이나 중간에 요소를 삽입할 수 있다.

```
v.add(Integer.valueOf(5));
v.add(5)
v.add(2, 100);
```

자동 박싱이 있으니 생략해도 된다.

인덱스 2의 위치에 정수 100을 삽입하라는 뜻이다.

벡터 내의 요소를 알아내기 위해서는 get( )이나 elementAt( ) 메소드를 이용한다. 괄호 안에 입력된 index를 받으면 index번째의 수를 출력한다.

벡터의 크기와 용량을 알아내기 위해서
