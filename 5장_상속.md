## 5.1 상속의 개념

### 클래스 사이의 상속
객체 지향에서 상속은 부모 클래스에 만들어진 필드와 메소드를 자식 클래스가 물려받는 것이다.

상속 선언만 하면, 자식 클래스는 부모 클래스에 만들어진 필드와 메소드를 만들지 않고도 만든 것과 같은 효과를 얻는다.

상속은 클래스 사이의 상속이지 객체 사이의 상속이 아니다.

상속은 코드 중복을 제거하여 클래스를 간결하게 구현할 수 있게 한다. 코드가 간결해 지면 유지 보수 측면에서도 유리하다.

[ 상속의 장점 ]
- 클래스의 간결화 : 멤버의 중복 작성 불필요
- 클래스 관리 용이 : 클래스들의 계층적 분류
- 소프트웨어의 생산성 향상 : 클래스 재사용과 확장 용이

## 5.2 클래스 상속과 객체

자바에서는 부모 클래스를 슈퍼 클래스, 상속받는 자식 클래스를 서브 클래스라고 부른다. 

상속을 선언할 때는 extends 키워드를 사용한다.

`public class 서브클래스 extends 슈퍼클래스` 의 형식으로 선언 가능하다.

상속받은 서브 클래스의 객체는 따로 적어두지 않아도 슈퍼클래스의 멤버를 가진다.

서브 클래스는 슈퍼 클래스의 private 멤버를 제외하고 모든 멤버를 접근할 수 있다.

[ 상속의 특징 ]
- 클래스를 여러 개 상속받는 다중 상속을 지원하지 않는다.
- 상속의 횟수에는 제한이 없다.
- 계층 구조의 최상위에 java.leng.Object 클래스가 있다. (별로 안중요한듯)

## 5.3 상속과 protected 접근 지정자

[ 슈퍼클래스 멤버의 접근 지정자 별 서브 클래스의 접근 가능 여부 ]
- private : 접근 불가능
- default : 같은 패키지에 있는 서브 클래스라면 가능하지만 다른 패키지의 서브 클래스라면 불가능
- public : 접근 가능
- protected : 어느 패키지에 있던 접근 가능

## 5.4 상속과 생성자

서브 클래스의 객체가 생성될 때는 슈퍼 클래스의 생성자와 서브 클래스의 생성자가 둘 다 실행된다.

서브 클래스의 객체가 생성 -> 서브 클래스의 생성자 호출 -> 슈퍼 클래스의 생성자 호출 

-> 슈퍼 클래스의 생성자 실행 -> 서브 클래스의 생성자 실행 

서브 클래스의 생성자가 호출되면 위로 올라가며 순서대로 호출되고 가장 마지막에 호출된 생성자부터 차례로 실행되며 다시 내려온다.

슈퍼 클래스의 생성자를 명시적으로 지정하지 않는 경우, 서브 클래스의 생성자가 기본 생성자던 매개변수를 가진 것이던, 슈퍼 클래스의 기본 생성자를 호출하도록 한다. (중요)

만약 지정을 안했는데 기본 생성자가 없다면 매개변수를 가진 다른 생성자가 있더라도 오류가 발생한다.

`super()`를 이용하면 명시적으로 슈퍼 클래스의 생성자를 지정할 수 있다. 반드시 생성자의 첫 라인에 사용되어야 한다는 점을 유의하자

## 5.5 업캐스팅과 instanceof 연산자

캐스팅이란 타입 변환을 말하는 것으로 이전 보다 큰 범위의 타입으로 늘리는 것을 업캐스팅 작은 범위의 타입으로 줄이는 것을 다운캐스팅이라고 한다.

### 업캐스팅

서브 클래스의 객체에 대한 레퍼런스를 슈퍼 클래스 타입으로 변환하는 것은 작은 서브에서 큰 슈퍼로 주소가 바뀌는 것이기에 업캐스팅이다.

업캐스팅한 레퍼런스로는 객체 내의 모든 멤버에 접근할 수 없고 슈퍼 클래스의 멤버만 접근 가능하다.

업캐스팅을 진행한 서브 클래스의 객체여도 슈퍼 클래스의 private 멤버에는 접근할 수 없다.

업캐스팅이 진행될 때는 원래 보다 공간이 넓어지는 것이기에 자동으로 타입이 변환되고 데이터 손실이 없다.

### 다운캐스팅

슈퍼 클래스의 객체에 대한 레퍼런스를 서브 클래스 타입으로 변환하는 것은 큰 슈퍼에서 작은 서브로 주소가 바뀌는 것이기에 다운캐스팅이다.

다운캐스팅이 진행될 때는 원래 보다 공간이 좁아지는 것이기에 업캐스팅과 달리 주소 앞에 슈퍼 클래스 이름을 붙여 명시적으로 타입변환을 지정해야한다.

`서브 클래스의 주소 = (서브 클래스 이름) 슈퍼 클래스의 주소` 의 형식으로 다운 캐스팅을 할 수 있다.

### instanceof 연산자

업캐스팅을 한 경우, 레퍼런스가 가리키는 객체의 진짜 클래스 타입을 구분하기 어려우니 구분하기 위해 사용한다.

`주소 instanceof 클래스 이름` 을 하면 주소가 가리키는 객체가 해당 클래스 타입의 객체인지를 판단해 T/F를 출력한다.

## 5.6 메소드 오버라이딩

### 메소드 오버라이딩

메소드 오버라이딩은 슈퍼 클래스와 서브 클래스의 메소드 사이에서 발생하는 관계로, 슈퍼 클래스에 선언된 메소드와 같은 이름, 같은 리턴 타입, 같은 매개변수 리스트를 갖는 메소드를 서브 클래스에 재작성 하는 것이다.

오버로딩과 오버라이딩은 자바에서 다형성을 이루는 방법들이다.

| 비교 요소 | 메소드 오버로딩 | 메소드 오버라이딩 |
|------|------|------|
| 선언 | 같은 클래스나 상속 관계에서 동일한 이름의 메소드 중복 작성 | 서브 클래스에서 슈퍼 클래스에 있는 메소드 동일한 이름의 메소드 재작성 |
| 관계 | 동일한 클래스 내 혹은 상속 관계 | 상속 관계 |
| 목적 | 이름이 같은 여러 개의 메소드를 중복 작성하여 사용의 편리성 향상. 다형성 실현 | 슈퍼 클래스에 구현된 메소드를 무시하고 서브 클래스에서 새로운 기능의 메소드를 재정의하고자 함. 다형성 실현 |
| 조건 | 메소드 이름은 반드시 동일하고, 매개변수 타입이나 개수가 달라야 성립 | 메소드의 이름, 매개변수 타입과 개수, 리턴 타입이 모두 동일해야 성림 |
| 바인딩 | 정적 바인딩. 호출될 메소드는 컴파일 시에 결정 | 동적 바인딩. 실행 시간에 오버라이딩된 메소드 찾아서 호출 |

`@Override`를 오버라이딩된 메소드 앞에 적어줌으로서, 컴파일러에게 오버라이딩이 정확한지를 확인하도록 지시하는 것으로 이걸 썼는데 메소드간 이름이 다르다면 금방 발견가능하다. (중요)

[ 메소드 오버라이딩의 제약 사항 ]
- 슈퍼 클래스의 메소드와 동일한 원형으로 작성해야한다.
- 슈퍼 클래스 메소드의 접근 지정자보다 접근 범위를 좁혀 오버라이딩 할 수 없다.
- static 이나 private 또는 final로 선언된 메소드는 서브 클래스에서 오버라이딩할 수 없다.

`super.슈퍼클래스의멤버`의 형태로 서브 클래스가 아닌 슈퍼 클래스의 멤버에 접근하고 싶을 때 접근할 수 있다.

[ 참고(중요) ]
- this 는 현재 객체의 모든 멤버에 접근 가능
- super 는 현재 객체 내에 있는 슈퍼 클래스 멤버에 접근 가능
- this() 는 생성자에서 다른 생성자를 호출할 때 사용
- super() 는 서브 클래스의 생성자에서 슈퍼 클래스의 생성자를 호출할 때 사용

### 동적 바인딩

슈퍼 클래스의 메소드를 무시하고 서브 클래스에서 오버라이딩된 메소드가 무조건 실행되도록 하는 것인데 이런 처리를 동적 바인딩이라고 부르며, 메소드 오버라이딩은 동적 바인딩을 유발시킨다.

업캐스팅된 주소로 오버라이딩된 메소드를 사용하면 동적 바인딩이 유발되고 무조건 오버라이딩된 서브 클래스의 메소드가 실행된다. (중요)

동적 바인딩은 실행할 메소드를 컴파일 시(compile time)에 결정하지 않고 실행 시(run time)에 결정한다. 이를 통해 오버라이딩된 메소드가 항상 실행되도록 보장한다. (중요)

## 5.7 추상 클래스

- 단순 상속 : 메소드의 재사용이 목적이 아닌 메소드의 타입을 물려주는 것이 목적인 상속 (ex 추상 클래스의 상속)
- 구현 상속 : 이미 구현되어 있는 메소드들을 서브 클래스에게 물려주어 그걸 그대로 사용하는 상속

### 추상 메소드

추상 메소드란 선언은 되어 있으나 코드가 구현되어 있지 않은 껍데기만 있는 메소드이다.

`abstract`키워드와 함께 원형만 선언하고 코드는 작성하지 않는다.

### 추상 클래스

`abstract`키워드로 선언하고 추상 메소드와 달리 코드가 작성되어도 된다. 클래스 안에 추상 메소드가 있어도 없어도 상관없다. 

클래스 안에 추상 메소드가 있다면 해당 클래스는 반드시 추상 클래스로 선언되어야만 한다.

추상 클래스는 본디 객체를 생성할 목적으로 만드는 클래스가 아니다. 추상 클래스에는 실행 코드가 없는 미완성 상태인 추상 메소드가 있을 수 있기에 추상 클래스에서는 객체를 생성할 수 없고 이를 그냥 실행하면 오류가 발생한다. 
객체를 생성하지는 않고 레퍼런스를 선언하는 것은 괜찮다. (중요)

추상 클래스를 상속 받는 서브 클래스는 추상 클래스가 되므로 `abstract`키워드를 붙여야 오류가 없다.

추상 클래스의 구현 이란 추상 클래스에 있는 모든 추상 메소드들을 서브 클래스에서 오버라이딩해 실행 가능한 코드로 구현하는 것을 말한다. 추상 클래스는 추상 메소드를 통해 서브 클래스가 구현할 메소드를 명료하게 알려주는 인터페이스의 역할을 하고,
서브 클래스는 추상 메소드를 목적에 맞게 구현하는 다형성을 실현할 수 있다.

추상 클래스를 이용하면 코드 내에서 프로그램의 설계와 구현을 분리할 수 있고 계층적 상속 관계를 가지는 클래스들의 구조를 만들 때 적합하다.

다중 상속이 불가능 하다.

## 5.8 인터페이스

인터페이스는 서로 다른 하드웨어 장치들이 상호 데이터를 주고받을 수 있는 규격을 의미한다.

목적 : 인터페이스는 스펙을 주어 클래스들이 그 기능을 서로 다르게 구현할 수 있도록 하는 클래스의 규격 선언이며, 클래스의 다형성을 실현하는 도구이다.

`interface`키워드를 사용해 클래스를 선언하듯 선언한다.

인터페이스는 필드(멤버 변수)를 만들 수 없고, 객체를 생성할 수도 없지만 레퍼런스 변수는 선언 가능하다.

[ 인터페이스의 구성 ]
- 상수
- 추상 메소드 : public abstract로 정해져 있으며 생략될 수 있고, 다른 접근 지정자로 지정될 수 없다.
- default 메소드 : 접근 지정이 public 으로 고정되고 인터페이스 내에 코드가 작성되어 있어야 한다.
- private 메소드 : 인터페이스 내에서만 호출이 가능하고 인터페이스 내에 코드가 작서오디어 있어야 한다.
- static 메소드 : 접근 지정이 생략되면 public 이며 private으로도 지정될 수 있다.

인터페이스는 상속 받을 서브 클래스에게 구현할 메소드들의 원형을 모두 알려주어 클래스가 스스로의 목적에 맞게 메소드를 구현하도록 하는 것이 목적이다.

인터페이스의 구현이란 `implements`키워드를 사용해 인터페이스의 모든 추상 메소드를 구현한 클래스를 작성하는 것을 말한다.

`class 클래스 이름 implements 인터페이스 이름`이 때 인터페이스 이름을 , 기준으로 여러 개 쓰면 다중 상속이 가능하다. 다중 상속을 진행했을 경우 모든 추상 메소드들을 구현해야한다.

인터페이스 끼리는 `extends`키워드로 서로 상속 하거나 받을 수 있다.

[ 추상 클래스와 인터페이스 ]
| 비교 | 목적 | 구성 |
| --- | --- | --- |
| 추상 클래스 | 서브 클래스에서 필요로하는 대부분의 기능을 구현해 놓고 상속 받아 활용할 수 있도록 하되, 서브 클래스에서 구현 할 수밖에 없는 기능만을 추상 메소드로 선언해, 서브 클래스에서 구현하도록 하는 목적(다형성) | 추상 메소드와 일반 메소드, 변수 필드 모두 포함 |
| 인터페이스 | 객체의 기능을 모두 공개한 표준화 문서와 같은 것. 인터페이스를 상속 받는 클래스의 목적에 따라 인터페이스의 모든 추상 메소드를 만들도록 하는 목적 (다형성) | 변수 필드( 멤버 변수 )는 포함 하지 않고 상수, 추상 메소드, 일반 메소드, default 메소드, static 메소드, 모두 포함하지만 protected 접근 지정 선언 불가 |
