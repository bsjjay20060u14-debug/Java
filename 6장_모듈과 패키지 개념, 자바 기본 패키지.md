## 6.1 패키지
### 패키지의 개념과 필요성
패키지란 서로 관련 있는 클래스나 인터페이스의 컴파일된 클래스 파일들을 한 곳에 묶어 놓은 것이다.

## 6.2 import와 클래스 경로
다른 패키지에 있는 클래스의 내용을 사용하려 하면 모든 경로를 사용할 때마다 적어주어야하는데 import문을 사용해 미리 선언하면 그럴 필요 없다.

`import 패키지.클래스`를 반드시 소스 코드의 앞부분에 작성해야한다.

`import 패키지.*`은 패키지 내의 여러 개의 클래스를 사용하려 할 때 그냥 패키지 자체를 import 하는 것이다.

## 6.3 패키지 만들기

`pakage 패키지 이름`을 소스 파일의 첫 줄에 선언한다. 

패키지 선언문을 사용하지 않고 클래스나 인터페이스를 작성하면 컴파일러는 이를 디폴트 패키지에 소속시킨다. 디폴드 패키지는 현재 디렉토리 이다.

이클립스로 쉽게 만들어도 된다.

[ 패키지의 특징 ]
- 패키지 계층 구조
- 패키지별 접근 제한 : 패키지를 접근 권한의 범위로도 이용할 수 있다.
- 동일한 이름의 클래스를 다른 패키지에 작성 가능
- 소프트웨어의 높은 재사용성 : 유사한 기능의 클래스나 인터페이스를 재작성할 필요가 없어 큰 규모의 프로젝트에서 큰 자산이 된다.

## 6.4 모듈 개념

패키지들은 모듈이라는 단위로 묶여 제공된다.

모듈은 패키지들을 담는 컨테이너로 모듈 파일(.jmd)로 저장한다.

모듈화의 가장 큰 목적은 컴포넌트들을 필요에 따라 조립해 사용하기 위함이다. 세밀한 모듈화로 시스템에 불필요한 부담을 줄인다.

## 6.5 자바 JDK에서 제공하는 패키지

[ 주요 패키지 ]
- java.lang : 특별히 import로 선언되지 않아도 자동으로 import된다.
- java.util : 날짜, 시간, 해시맵 등을 제공
- java.io : 키보드, 모니터, 파일 등에 입출력하는 기능 제공
- java.awt 와 javax.swing : GUI 프로그래밍에 필요한 기능 제공

## 6.6 Object 클래스

Object는 java.lang에 속한 클래스로 모든 클래스에 강제로 상속된다.

모든 객체에게 공통적으로 있어야 할 메소드를 포함한다.

- getClass( ) : 레퍼런스가 가리키는 객체를 리
- getClass( ).getName( ) : 레퍼런스가 가리키는 객체의 클래스 타입을 리턴
- hashCode( ) : 객체 안에 단겨진 해시코드 값을 리턴
- toString( ) : 객페를 문자열로 변환해 리턴. 오버라이딩해 자신만의 문자열을 리턴할 수 있음

### 객체 비교와 equals( ) 메소드
equals() 메소드는 두 객체의 내용이 같은지를 비교하는 메소드로 객체들을 비교할 때 사용한다.

== 연산자는 두 객체의 내용물이 같은지 비교하는 것이 아니라, 두 레퍼런스가 동일한 객체를 가리키는지를 비교한다. (355p)

## 6.7 Wrapper 클래스
### Wrapper 클래스 개념
이름이 Wrapper인 클래스는 존재하지 않는다. 

Byte, Short, Integer, Long, Character, Douable, Float, Boolean 의 8개의 기본 타입의 값을 객체로 만들 어 사용할 수 있도록 하는 클래스를 통칭하여 Wrapper 클래스라고 한다.

### Wrapper 클랫므의 객체 생성

정적 메소드인 valueOf( )를 호출해 생성한다.

```
Integer i = Integer.valuOf(10);

Charater c = Character.valueOf('c');

Integer i = Integer.valueOf("10");
```

Character를 제외하고 " "을 통해 문자열로 Wrapper객체를 생성할 수도 있다. Character는 " " 가 아니라 ' '이기 때문

많은 메소드가 static 타입이다.

[ Wrapper 클래스의 활용 ]
- Wrapper 객체에 들어 있는 기본 타입 값 알아내기 : 레퍼런스 변수.타입Value( )로 가능하다
```
Integer i = Integer.valueOf(10);
int ii = i.intValue( );
```
- 문자열을 기본 타입으로 변환 : 해당 타입의 Wrapper 클래스.Parse해당 타입( 값 ) 으로 가능하다
```
double d = Doauble.parseDouble("3.14");
```
- 기본 타입 값을 문자열로 변환 : Wrapper 클래스.toString( 값 ) 으로 가능하다.
```
String  s = Character.toString('a');  // 'a'에서 "a"로 변환
```

### 박싱과 언박싱
박싱은 기본 타입의 값을 Wrapper 객체로 변환하는 것이고 ( 자동 박싱에 의해 valueof( )를 생략해도 알아서 해준다. )

언박싱은 Wrapper 객체를 기본 타입의 값으로 변환하는 것이다. ( 자동 언박싱에 의해 타입Value( )를 생략해도 알아서 해준다. )

## 6.8 String 클래스
### String의 특징과 객체 생성
java.lang 패키지에 포함된 클래스로 String 클래스는 문자열을 나타낸다.

스트링 리터럴과 new String ( ) 로 생성된 스트링 객체는 다르게 관리된다.  스트링 리터럴은 자바 내부에서 리터럴 테이블로 특별히 관리해, 동일한 리터럴을 공유시키지만
new String( )으로 생성된 스트링은 new를 이용해 생성되는 다른 객체들 처럼 힙 메모리에 독립적으로 생성되고, 스트링 객체는 수정이 불가능 하다. (중요)

[ String의 활용 ]
- 문자열 비교 : int compareTo(String anotherString) 괄호와 비교해 같으면 0 현재 문자열이 사전 순으로 앞이면 음수 뒤면 양수를 리턴
- 문자열 연결 : String concat(String str) 기존의 문자열에 새 문자를 추가하면, 기존 문자열은 가비지가 되고 추가된 전체의 문자열이 남는다. 비슷한 역할로 +연산자가 있다
- 공백 제거 : String trim( )
- 문자열의 문자 : char charAt(int index) 문자열의 index 번째의 문자를 리턴. charAt( )과 쓰면 문자가 몇 번 쓰였는지도 알 수 있음

## 6.9 StringBuffer 클래스
### StringBuffer의 생성과 특징
java.lang 패키지에 포함된 클래스로 StringBuffer 객체는 문자열을 저장하는 가변 버퍼를 가지고 있기 때문에 저장된 문자열의 수정이 가능하다.

 입력값에 따라 달라지는 가변 버퍼 이기 때문에 .length로 값을 알 수 없음

`StringBuffer sb = new StringBuffer("java");`

### StringBuffer의 활용
표 (373p)

간단한 문자열은 String 클래스를 이용하고, 문자열의 길이가 길거나 문자열이 수시로 변하는 경우에 StringBuffer 클래스를 이용하는 것이 적합하다.

## 6.10 StringTokenizer 클래스
### StringTokenizer의 생성과 특징
java.util 패키지에 포함되어 있는 클래스로 하나의 문자열을 여러 개의 문자열로 분리하기 위해 사용된다. 

문자열을 분리할 때 사용되는 기준 문자를 구분 문자라고 하고, 구분 문자로 분리된 문자열을 토큰이라고 한다. 구분문자를 띄어쓰기 없이 여러 개 사용할 수도 있다.

```
String query = "name&kitae";
StringTokenizer st = new StringTokenizer(query, "&");
```

query의 문자열을 &기준으로 나눠 st라는 객체에 토큰으로 나누어 각각 저장하라는 뜻이다.

String 클래스의 split( ) 메소드가 같은 역할을 한다.

표(377p)
## 6.11 Math 클래스
## 6.12 Calendar 클래스
